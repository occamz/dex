services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dex_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5499:5432"
    tmpfs:
      - /var/lib/postgresql/data
